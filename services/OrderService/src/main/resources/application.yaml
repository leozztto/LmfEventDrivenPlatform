spring:
  application:
    name: OrderService

  datasource:
    url: jdbc:postgresql://localhost:5432/orderservice
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

  kafka:
    bootstrap-servers: localhost:9092
